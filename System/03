Il y a plusieurs fichiers a modifier,

d'abord le fichier hostname ("/etc/hostname")

ensuite dans le fichier hosts ("/etc/host")  il reste des references
a notre ancien hostname, il faut les remplacer par le nouvel hostname

